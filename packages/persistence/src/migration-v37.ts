/**
 * Kept as a small module as well as a schema export so migration-specific
 * tests can import the version without loading the complete schema module.
 */
export { MIGRATION_V37 } from './schema.js';
